/**
 * 1. Preprocessing:
 * Use preprocessor directive to find relevant code,
 * then copy and paste it to the current source file.
 *
 * 2. Compiling:
 * Make any source code as assembly code,
 * providing instructions for the CPU.
 */

#include <stdio.h>

int main(void)
{
    printf("> hello, world\n");

    return 0;
}

/**
 * 3. Assembling:
 * Make the assembly code as machine code,
 * commonly binary code, readable by the CPU.
 *
 * 4. Linking:
 * Combine every objects from the assembling process into an executable.
 */